#!/bin/sh
# Block any .env files from being committed — catches accidental secret leaks.

if git diff --cached --name-only | grep -qE '(^|/)\.env$'; then
  echo "ERROR: Attempt to commit a .env file detected."
  echo "Remove it from staging: git restore --staged .env"
  exit 1
fi
